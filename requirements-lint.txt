# Minimal dependencies for the lint CI job (ruff + mypy).
# Intentionally excludes all ML packages (torch, transformers, etc.) — mypy
# skips them via ignore_missing_imports = true in pyproject.toml.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
httpx==0.27.2
asyncpg==0.29.0
redis[asyncio]==5.0.8
pyyaml==6.0.2
typer==0.12.5
rich==13.8.1
prometheus-client>=0.21.0
python-json-logger>=2.0.7
google-genai>=1.0.0
openai>=1.50.0
anthropic>=0.37.0
